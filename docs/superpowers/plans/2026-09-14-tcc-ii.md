# TCC II (Bloco F) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar o ciclo inteiro do curso: o TCC II nasce automaticamente
quando o TCC I é concluído (ou manualmente, pra equivalência externa),
reaproveita `Submissao`/`Banca`/`Ata` (já genéricos por `Projeto`), e
acrescenta o checklist de correções e o termo de publicação como
pré-requisitos da mesma transição `Aprovado com Ressalvas → Aprovado`.

**Architecture:** Duas extensões pequenas de apps existentes
(`apps/projetos`: `Projeto.anterior`/`coorientador`, `TermoPublicacao`,
`criar_tcc_ii_*`; `apps/bancas`: `ItemCorrecao`) — nenhum app novo. Reaproveita
ao máximo serviços já testados (`criar_projeto_sob_limite`,
`projeto_ativo_do_aluno`, `agendar_banca`, `gerar_ata`) em vez de duplicar
lógica.

**Tech Stack:** Django 5, PostgreSQL, Celery, pytest-django, Playwright + axe-core.

**Spec:** `docs/superpowers/specs/2026-09-14-tcc-ii-design.md`

## Global Constraints

- Todo identificador de código é em português; interface, mensagens e commits também.
- Regra de negócio nunca em `views.py`/`models.py` — só em `services.py` de cada
  app (CLAUDE.md, regra 4).
- Permissão é POSSE em toda checagem deste bloco (`usuario == projeto.orientador`
  ou `usuario == projeto.aluno`, conforme a ação) — nenhuma checagem nova por papel.
- Lookup de view sempre ESCOPADO ao dono (`get_object_or_404(Modelo, pk=...,
  campo_de_posse=request.user)`): dono alheio e pk inexistente respondem os
  DOIS com 404, nunca 403.
- **Disciplina de testes (CLAUDE.md):** toda checagem nova provada por mutação
  (comente a checagem, confirme que algum teste reprova, desfaça); uma checagem
  vizinha pode mascarar a ausência da nova — nunca inferir cobertura pela
  suíte passando; toda citação de arquivo:linha conferida no momento em que é
  escrita.
- Toda tela nova entra em `conftest.py::ROTAS` (nunca numa suíte de
  acessibilidade isolada) para receber as cinco checagens transversais.
- `CheckConstraint` usa `condition=`, nunca `check=` (Django 5.2+ depreciou
  `check=` — achado real do Bloco D).
- `docker compose exec web pytest` / `ruff check .` / `black --check .` depois
  de cada tarefa.

---

## Mapa de arquivos

- **Modificar** `apps/projetos/models.py` — `Projeto.anterior`/`coorientador`/
  `coorientador_externo` + `CheckConstraint`; `TermoPublicacao` (novo modelo).
- **Modificar** `apps/bancas/models.py` — `ItemCorrecao` (novo modelo).
- **Modificar** `apps/projetos/admin.py`, `apps/bancas/admin.py` — registro
  dos modelos novos.
- **Modificar** `apps/projetos/services.py` — `criar_tcc_ii_automatico`,
  `criar_tcc_ii_manual`, `assinar_termo_publicacao`; `aprovar_projeto` ganha
  o gate do TCC_II; `meu_tcc` (view) passa a tentar TCC_II antes de TCC_I.
- **Modificar** `apps/projetos/permissions.py` — `pode_criar_tcc_ii_manual`,
  `pode_assinar_termo`.
- **Modificar** `apps/bancas/services.py` — `criar_item_correcao`,
  `concluir_item_correcao`.
- **Modificar** `apps/bancas/permissions.py` — `pode_gerenciar_correcao`.
- **Modificar** `apps/documentos/services.py` — `aprovar_ata` chama
  `criar_tcc_ii_automatico` condicionalmente.
- **Criar** `apps/projetos/tasks.py` (extensão) — `enviar_tcc_ii_criado`.
- **Criar** `apps/bancas/tasks.py` (extensão) — `enviar_item_correcao_criado`.
- **Modificar** `apps/projetos/forms.py` — `FormularioCriarTccII`.
- **Criar** `apps/bancas/forms.py` (extensão) — `FormularioItemCorrecao`.
- **Modificar** `apps/projetos/views.py` — `criar_tcc_ii_manual_view`,
  `meu_tcc` estendida, `aprovar_projeto_view` corrigida (captura
  `ValidationError`, hoje sem tratamento — achado real deste bloco).
- **Modificar** `apps/bancas/views.py` — `correcoes`, `concluir_item_view`.
- **Modificar** `apps/projetos/urls.py`, `apps/bancas/urls.py`.
- **Modificar** `templates/projetos/meu_tcc.html`,
  `templates/projetos/orientacoes.html`.
- **Criar** `templates/projetos/criar_tcc_ii.html`,
  `templates/bancas/correcoes.html`.
- **Criar** `templates/email/tcc_ii_criado.txt`,
  `templates/email/item_correcao_criado.txt`.
- **Modificar** `conftest.py` — `ROTAS` (duas telas novas).
- **Testes:** `apps/projetos/tests/test_tcc_ii.py`,
  `apps/bancas/tests/test_correcoes.py`, mais extensões pontuais em arquivos
  existentes.

---

## Tarefa 1: Modelos — `Projeto.anterior`/`coorientador`, `TermoPublicacao`, `ItemCorrecao`

**Arquivos:**
- Modificar: `apps/projetos/models.py`, `apps/bancas/models.py`,
  `apps/projetos/admin.py`, `apps/bancas/admin.py`.
- Migrações: `apps/projetos/migrations/`, `apps/bancas/migrations/`.
- Teste: `apps/projetos/tests/test_tcc_ii.py` (novo, começa com os testes de
  modelo), `apps/bancas/tests/test_correcoes.py` (novo).

**Interfaces:**
- Produz: `Projeto.anterior`, `Projeto.coorientador`,
  `Projeto.coorientador_externo`; `TermoPublicacao` (`projeto`, `assinado_em`);
  `ItemCorrecao` (`projeto`, `descricao`, `concluido`, `criado_em`).

- [ ] **Passo 1: Escrever os testes de modelo (falhando)**

Crie `apps/projetos/tests/test_tcc_ii.py`:

```python
"""Testes do Bloco F: TCC II. Modelos (`Projeto.anterior`/`coorientador`,
`TermoPublicacao`) nesta primeira parte; serviços nas tarefas seguintes."""

import pytest

from apps.contas.models import PerfilAluno, PerfilProfessor, Usuario
from apps.contas.validators import _digito
from apps.projetos.models import Projeto, TermoPublicacao


def _cpf(indice):
    """CPF sintético com dígitos verificadores válidos, faixa 800000000+ —
    livre (conferida por grep) para os testes do Bloco F."""
    base = f"{800000000 + indice:09d}"
    d1 = _digito(base, 10)
    d2 = _digito(base + str(d1), 11)
    return base + str(d1) + str(d2)


def _professor(indice, nome):
    usuario = Usuario.objects.create_user(
        email=f"professor.tccii.{indice}@ufsm.br",
        password="x",
        nome_completo=nome,
        cpf=_cpf(indice),
    )
    return PerfilProfessor.objects.create(usuario=usuario, siape=f"TCCII{indice:03d}")


def _aluno(indice, nome):
    usuario = Usuario.objects.create_user(
        email=f"aluno.tccii.{indice}@ufsm.br",
        password="x",
        nome_completo=nome,
        papel=Usuario.ALUNO,
        cpf=_cpf(indice),
    )
    PerfilAluno.objects.create(usuario=usuario, matricula=f"2026TCCII{indice:02d}")
    return usuario


@pytest.fixture
def projeto_tcc_i(db):
    orientador = _professor(1, "Orientador TCC II Modelo")
    aluno = _aluno(2, "Aluno TCC II Modelo")
    return Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        etapa=Projeto.TCC_I,
        status=Projeto.CONCLUIDO,
        ano=2026,
        periodo=1,
    )


@pytest.mark.django_db
def test_projeto_tcc_ii_aponta_para_anterior(projeto_tcc_i):
    tcc_ii = Projeto.objects.create(
        aluno=projeto_tcc_i.aluno,
        orientador=projeto_tcc_i.orientador,
        etapa=Projeto.TCC_II,
        status=Projeto.EM_ANDAMENTO,
        anterior=projeto_tcc_i,
        ano=2026,
        periodo=2,
    )
    assert tcc_ii.anterior_id == projeto_tcc_i.id


@pytest.mark.django_db
def test_projeto_coorientador_recusa_interno_e_externo_juntos(projeto_tcc_i):
    coorientador = _professor(3, "Coorientador Duplo")
    with pytest.raises(Exception):
        Projeto.objects.create(
            aluno=_aluno(4, "Aluno Coorientador Duplo"),
            orientador=projeto_tcc_i.orientador,
            etapa=Projeto.TCC_I,
            status=Projeto.EM_ANDAMENTO,
            coorientador=coorientador,
            coorientador_externo="Fulano Externo",
            ano=2026,
            periodo=1,
        )


@pytest.mark.django_db
def test_projeto_coorientador_aceita_so_um_ou_nenhum(projeto_tcc_i):
    coorientador = _professor(5, "Coorientador Único")
    projeto = Projeto.objects.create(
        aluno=_aluno(6, "Aluno Coorientador Único"),
        orientador=projeto_tcc_i.orientador,
        etapa=Projeto.TCC_I,
        status=Projeto.EM_ANDAMENTO,
        coorientador=coorientador,
        ano=2026,
        periodo=1,
    )
    assert projeto.coorientador_id == coorientador.id
    assert projeto.coorientador_externo == ""


@pytest.mark.django_db
def test_termo_publicacao_existencia_significa_assinado(projeto_tcc_i):
    termo = TermoPublicacao.objects.create(projeto=projeto_tcc_i)
    assert termo.assinado_em is not None
```

Crie `apps/bancas/tests/__init__.py` já existe (Bloco D); crie
`apps/bancas/tests/test_correcoes.py`:

```python
"""Testes do Bloco F: `ItemCorrecao` (modelo nesta primeira parte, serviços
na Tarefa 4)."""

import pytest

from apps.bancas.models import ItemCorrecao
from apps.contas.models import PerfilAluno, PerfilProfessor, Usuario
from apps.contas.validators import _digito
from apps.projetos.models import Projeto


def _cpf(indice):
    base = f"{810000000 + indice:09d}"
    d1 = _digito(base, 10)
    d2 = _digito(base + str(d1), 11)
    return base + str(d1) + str(d2)


@pytest.fixture
def projeto_tcc_ii(db):
    orientador = Usuario.objects.create_user(
        email="professor.correcao.modelo@ufsm.br",
        password="x",
        nome_completo="Professor Correção Modelo",
        cpf=_cpf(1),
    )
    PerfilProfessor.objects.create(usuario=orientador, siape="CORR0001")
    aluno = Usuario.objects.create_user(
        email="aluno.correcao.modelo@ufsm.br",
        password="x",
        nome_completo="Aluno Correção Modelo",
        papel=Usuario.ALUNO,
        cpf=_cpf(2),
    )
    PerfilAluno.objects.create(usuario=aluno, matricula="2026CORR001")
    return Projeto.objects.create(
        aluno=aluno,
        orientador=orientador,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=1,
    )


@pytest.mark.django_db
def test_item_correcao_nasce_nao_concluido(projeto_tcc_ii):
    item = ItemCorrecao.objects.create(projeto=projeto_tcc_ii, descricao="Ajustar a conclusão.")
    assert item.concluido is False
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py apps/bancas/tests/test_correcoes.py -v`
Expected: `ImportError`/`AttributeError` — `Projeto.anterior`,
`TermoPublicacao`, `ItemCorrecao` não existem ainda.

- [ ] **Passo 3: Estender `apps/projetos/models.py`**

Na classe `Projeto`, depois do campo `periodo` e antes de `criado_em`
(confira a posição exata lendo o arquivo — este spec foi escrito antes de
qualquer edição do Bloco F nele):

```python
    anterior = models.ForeignKey(
        "self",
        # SET_NULL: apagar o TCC I não pode impedir a consulta ao TCC II
        # que restou — o vínculo é só um ponteiro histórico, não uma
        # dependência de integridade acadêmica.
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="proximos",
        verbose_name="projeto anterior",
    )
    coorientador = models.ForeignKey(
        PerfilProfessor,
        on_delete=models.PROTECT,
        null=True,
        blank=True,
        related_name="projetos_como_coorientador",
        verbose_name="coorientador",
    )
    coorientador_externo = models.CharField(
        "coorientador (externo)", max_length=200, blank=True, default=""
    )
```

E na `Meta.constraints` (mesma classe), acrescente à lista já existente:

```python
            models.CheckConstraint(
                condition=(
                    models.Q(coorientador__isnull=True, coorientador_externo="")
                    | (models.Q(coorientador__isnull=False) & models.Q(coorientador_externo=""))
                    | (models.Q(coorientador__isnull=True) & ~models.Q(coorientador_externo=""))
                ),
                name="projeto_coorientador_nao_duplo",
            ),
```

Confirme que `PerfilProfessor` já está importado no topo do arquivo (é —
`Tema.professor` já o usa).

Ao final do arquivo, acrescente o novo modelo:

```python
class TermoPublicacao(models.Model):
    """Aceite de publicação do TCC II no catálogo público — a EXISTÊNCIA da
    linha já significa "assinado" (Bloco F, spec §3.4): sem campo booleano
    redundante, sem estado intermediário. Ao contrário de `RevisaoSUGRAD`
    (Bloco E), que pode ser revisitada, assinar o termo é uma ação única,
    sem volta.
    """

    projeto = models.OneToOneField(
        Projeto,
        on_delete=models.PROTECT,
        related_name="termo_publicacao",
        verbose_name="projeto",
    )
    assinado_em = models.DateTimeField("assinado em", auto_now_add=True)

    class Meta:
        verbose_name = "termo de publicação"
        verbose_name_plural = "termos de publicação"

    def __str__(self):
        return f"Termo de {self.projeto} — assinado em {self.assinado_em:%d/%m/%Y}"
```

- [ ] **Passo 4: Acrescentar `ItemCorrecao` a `apps/bancas/models.py`**

Ao final do arquivo:

```python
class ItemCorrecao(models.Model):
    """Um item do checklist de correções pós-banca do TCC II (Bloco F,
    spec §4.2) — digitado pelo orientador com base no que a banca pediu
    (`Banca.comentario`, Bloco D), não estruturado a partir da banca em si
    (decisão do brainstorming: a banca só tem nota+comentário geral, sem
    itens individuais).
    """

    projeto = models.ForeignKey(
        Projeto,
        on_delete=models.PROTECT,
        related_name="itens_correcao",
        verbose_name="projeto",
    )
    descricao = models.TextField("descrição")
    concluido = models.BooleanField("concluído", default=False)
    criado_em = models.DateTimeField("criado em", auto_now_add=True)

    class Meta:
        verbose_name = "item de correção"
        verbose_name_plural = "itens de correção"
        ordering = ["criado_em"]

    def __str__(self):
        return f"{self.projeto} — {self.descricao[:50]}"
```

- [ ] **Passo 5: Registrar nos admins**

Em `apps/projetos/admin.py`, acrescente ao import de `apps.projetos.models`
`TermoPublicacao`, e registre:

```python
@admin.register(TermoPublicacao)
class TermoPublicacaoAdmin(admin.ModelAdmin):
    list_display = ["projeto", "assinado_em"]
    readonly_fields = ["assinado_em"]
```

Em `apps/bancas/admin.py`, acrescente ao import `ItemCorrecao`, e registre:

```python
@admin.register(ItemCorrecao)
class ItemCorrecaoAdmin(admin.ModelAdmin):
    list_display = ["projeto", "descricao", "concluido", "criado_em"]
    list_filter = ["concluido"]
    readonly_fields = ["criado_em"]
```

- [ ] **Passo 6: Gerar e aplicar as migrações**

Run: `docker compose exec web python manage.py makemigrations projetos bancas`
Expected: uma migração nova em cada app.

Run: `docker compose exec web python manage.py migrate`
Expected: aplica sem erro.

- [ ] **Passo 7: Rodar os testes e confirmar que passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py apps/bancas/tests/test_correcoes.py -v`
Expected: todos `PASSED`.

- [ ] **Passo 8: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/projetos/models.py apps/bancas/models.py apps/projetos/admin.py \
        apps/bancas/admin.py apps/projetos/migrations/ apps/bancas/migrations/ \
        apps/projetos/tests/test_tcc_ii.py apps/bancas/tests/test_correcoes.py
git commit -m "Tarefa 1: modelos Projeto.anterior/coorientador, TermoPublicacao, ItemCorrecao"
```

---

## Tarefa 2: `criar_tcc_ii_automatico`

**Arquivos:**
- Modificar: `apps/projetos/services.py`, `apps/documentos/services.py`.
- Teste: `apps/projetos/tests/test_tcc_ii.py` (extensão),
  `apps/documentos/tests/test_revisao.py` (extensão).

**Interfaces:**
- Produz: `apps.projetos.services.criar_tcc_ii_automatico(projeto_tcc_i) -> Projeto`.
- Consumido por: `apps.documentos.services.aprovar_ata` (Bloco E).

- [ ] **Passo 1: Escrever os testes (falhando)**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
from apps.projetos import services


@pytest.mark.django_db
def test_criar_tcc_ii_automatico_copia_aluno_e_orientador(projeto_tcc_i):
    tcc_ii = services.criar_tcc_ii_automatico(projeto_tcc_i)
    assert tcc_ii.aluno_id == projeto_tcc_i.aluno_id
    assert tcc_ii.orientador_id == projeto_tcc_i.orientador_id
    assert tcc_ii.etapa == Projeto.TCC_II
    assert tcc_ii.status == Projeto.EM_ANDAMENTO
    assert tcc_ii.anterior_id == projeto_tcc_i.id


@pytest.mark.django_db
def test_criar_tcc_ii_automatico_copia_coorientador(projeto_tcc_i):
    coorientador = _professor(7, "Coorientador Copiado")
    projeto_tcc_i.coorientador = coorientador
    projeto_tcc_i.save()
    tcc_ii = services.criar_tcc_ii_automatico(projeto_tcc_i)
    assert tcc_ii.coorientador_id == coorientador.id


@pytest.mark.django_db
def test_criar_tcc_ii_automatico_nao_checa_limite_de_vagas(projeto_tcc_i):
    """Mutação obrigatória (spec §3.1): este teste prova a AUSÊNCIA da
    checagem de vaga. Cria 3 outros TCC_II EM_ANDAMENTO para o mesmo
    orientador (o teto padrão) antes de chamar `criar_tcc_ii_automatico` —
    se a função checasse limite, este quarto TCC_II seria recusado."""
    from apps.comum.semestre import semestre_vigente

    ano, periodo = semestre_vigente()
    orientador = projeto_tcc_i.orientador
    for indice in (10, 11, 12):
        Projeto.objects.create(
            aluno=_aluno(indice, f"Aluno Vaga Cheia {indice}"),
            orientador=orientador,
            etapa=Projeto.TCC_II,
            status=Projeto.EM_ANDAMENTO,
            ano=ano,
            periodo=periodo,
        )
    tcc_ii = services.criar_tcc_ii_automatico(projeto_tcc_i)
    assert tcc_ii.pk is not None
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k criar_tcc_ii_automatico -v`
Expected: `AttributeError: module 'apps.projetos.services' has no attribute
'criar_tcc_ii_automatico'`.

- [ ] **Passo 3: Acrescentar `criar_tcc_ii_automatico` a `apps/projetos/services.py`**

Ao final do arquivo:

```python
def criar_tcc_ii_automatico(projeto_tcc_i):
    """Cria o TCC II a partir de um TCC I `Concluído` (Bloco F, spec §3.1)
    — chamada por `apps.documentos.services.aprovar_ata` no momento em que
    o TCC I vira `CONCLUIDO`. Copia aluno, orientador e coorientador; NÃO
    checa limite de vagas (é continuação de um aluno que o professor já
    orienta, não um compromisso novo — ao contrário de
    `criar_tcc_ii_manual`, que reaproveita `criar_projeto_sob_limite`)."""
    ano, periodo = semestre_vigente()
    return Projeto.objects.create(
        aluno=projeto_tcc_i.aluno,
        orientador=projeto_tcc_i.orientador,
        coorientador=projeto_tcc_i.coorientador,
        coorientador_externo=projeto_tcc_i.coorientador_externo,
        etapa=Projeto.TCC_II,
        status=Projeto.EM_ANDAMENTO,
        anterior=projeto_tcc_i,
        ano=ano,
        periodo=periodo,
    )
```

- [ ] **Passo 4: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k criar_tcc_ii_automatico -v`
Expected: todos `PASSED`.

- [ ] **Passo 5: Escrever o teste de integração com `aprovar_ata` (falhando)**

Acrescente a `apps/documentos/tests/test_revisao.py` (arquivo já existente,
Bloco E — reaproveite a fixture `ata_pendente`/`sugrad` já definidas lá):

```python
@pytest.mark.django_db
def test_aprovar_ata_cria_tcc_ii_quando_etapa_e_tcc_i(ata_pendente, sugrad):
    from apps.projetos.models import Projeto

    assert ata_pendente.projeto.etapa == Projeto.TCC_I
    services.aprovar_ata(ata_pendente, por=sugrad)
    tcc_ii = Projeto.objects.get(anterior=ata_pendente.projeto)
    assert tcc_ii.etapa == Projeto.TCC_II
    assert tcc_ii.aluno_id == ata_pendente.projeto.aluno_id


@pytest.mark.django_db
def test_aprovar_ata_nao_cria_tcc_iii_para_tcc_ii_concluido(sugrad):
    from apps.bancas.models import Banca
    from apps.contas.models import PerfilAluno, PerfilProfessor, Usuario
    from apps.contas.validators import _digito
    from apps.projetos.models import Projeto
    from django.utils import timezone

    def cpf(indice):
        base = f"{820000000 + indice:09d}"
        d1 = _digito(base, 10)
        d2 = _digito(base + str(d1), 11)
        return base + str(d1) + str(d2)

    aluno = Usuario.objects.create_user(
        email="aluno.tccii.ata@ufsm.br",
        password="x",
        nome_completo="Aluno TCC II Ata",
        papel=Usuario.ALUNO,
        cpf=cpf(1),
    )
    PerfilAluno.objects.create(usuario=aluno, matricula="2026TCCIIATA1")
    orientador = Usuario.objects.create_user(
        email="orientador.tccii.ata@ufsm.br",
        password="x",
        nome_completo="Orientador TCC II Ata",
        cpf=cpf(2),
    )
    PerfilProfessor.objects.create(usuario=orientador, siape="TCCIIATA1")
    projeto_tcc_ii = Projeto.objects.create(
        aluno=aluno,
        orientador=orientador,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO,
        ano=2026,
        periodo=1,
    )
    Banca.objects.create(
        projeto=projeto_tcc_ii,
        data_hora=timezone.now(),
        local="Sala 1",
        status=Banca.REALIZADA,
        nota=9.0,
        resultado=Projeto.APROVADO_COM_RESSALVAS,
    )
    ata = services.gerar_ata(projeto_tcc_ii)
    total_antes = Projeto.objects.count()
    services.aprovar_ata(ata, por=sugrad)
    assert Projeto.objects.count() == total_antes
```

- [ ] **Passo 6: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/documentos/tests/test_revisao.py -k tcc_ii -v`
Expected: `test_aprovar_ata_cria_tcc_ii_quando_etapa_e_tcc_i` falha
(`Projeto.DoesNotExist`) — `aprovar_ata` ainda não chama
`criar_tcc_ii_automatico`. `test_aprovar_ata_nao_cria_tcc_iii...` passa à
toa (nada cria nada ainda).

- [ ] **Passo 7: Plugar `criar_tcc_ii_automatico` em `aprovar_ata`**

Em `apps/documentos/services.py`, dentro de `aprovar_ata`, depois de
`ata.projeto.save(update_fields=["status"])`:

```python
    if ata.projeto.etapa == Projeto.TCC_I:
        from apps.projetos.services import criar_tcc_ii_automatico

        criar_tcc_ii_automatico(ata.projeto)
```

(Import local — mesmo motivo de `gerar_ata`/`anexar_banca_ativa`: evita que
`apps/documentos` dependa de `apps/projetos.services` no carregamento do
módulo. `apps/documentos/services.py` já importa `Projeto` de
`apps.projetos.models` no topo — confirme antes de duplicar o import.)

- [ ] **Passo 8: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/documentos/tests/test_revisao.py -k tcc_ii -v`
Expected: todos `PASSED`.

- [ ] **Passo 9: Rodar a suíte de `apps/documentos` inteira**

Run: `docker compose exec web pytest apps/documentos/ -v`
Expected: todos `PASSED` (nenhuma regressão nos testes de `aprovar_ata` do
Bloco E, que usam projetos `TCC_I` e agora também disparam a criação do
TCC II — confira se algum teste do Bloco E precisa de ajuste porque passou
a existir um `Projeto` a mais depois de `aprovar_ata`).

- [ ] **Passo 10: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/projetos/services.py apps/documentos/services.py \
        apps/projetos/tests/test_tcc_ii.py apps/documentos/tests/test_revisao.py
git commit -m "Tarefa 2: criar_tcc_ii_automatico"
```

---

## Tarefa 3: `criar_tcc_ii_manual`

**Arquivos:**
- Modificar: `apps/projetos/services.py`, `apps/projetos/permissions.py`,
  `apps/projetos/forms.py`, `apps/projetos/views.py`, `apps/projetos/urls.py`.
- Criar: `templates/projetos/criar_tcc_ii.html`.
- Teste: `apps/projetos/tests/test_tcc_ii.py` (extensão).

**Interfaces:**
- Consome: `criar_projeto_sob_limite(aluno, professor, tema, etapa)`
  (`apps/projetos/services.py:68`, Bloco B — sem alteração).
- Produz: `services.criar_tcc_ii_manual(aluno, professor, por) -> Projeto`;
  `permissions.pode_criar_tcc_ii_manual(usuario, professor) -> bool`.

- [ ] **Passo 1: Escrever os testes de serviço (falhando)**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
from django.core.exceptions import PermissionDenied, ValidationError

from apps.projetos import permissions


@pytest.mark.django_db
def test_criar_tcc_ii_manual_cria_sem_anterior():
    orientador = _professor(20, "Orientador Manual")
    aluno = _aluno(21, "Aluno Manual")
    tcc_ii = services.criar_tcc_ii_manual(aluno.perfil_aluno, orientador, por=orientador.usuario)
    assert tcc_ii.anterior is None
    assert tcc_ii.etapa == Projeto.TCC_II
    assert tcc_ii.orientador_id == orientador.usuario_id


@pytest.mark.django_db
def test_criar_tcc_ii_manual_recusa_quem_nao_e_o_professor():
    orientador = _professor(22, "Orientador Manual Dois")
    outro = _professor(23, "Outro Professor Manual")
    aluno = _aluno(24, "Aluno Manual Dois")
    with pytest.raises(PermissionDenied):
        services.criar_tcc_ii_manual(aluno.perfil_aluno, orientador, por=outro.usuario)


@pytest.mark.django_db
def test_criar_tcc_ii_manual_recusa_professor_no_limite():
    from apps.comum.semestre import semestre_vigente

    orientador = _professor(25, "Orientador Manual Limite")
    ano, periodo = semestre_vigente()
    for indice in (26, 27, 28):
        Projeto.objects.create(
            aluno=_aluno(indice, f"Aluno Limite Manual {indice}"),
            orientador=orientador.usuario,
            etapa=Projeto.TCC_II,
            status=Projeto.EM_ANDAMENTO,
            ano=ano,
            periodo=periodo,
        )
    aluno_novo = _aluno(29, "Aluno Manual Recusado")
    with pytest.raises(ValidationError):
        services.criar_tcc_ii_manual(
            aluno_novo.perfil_aluno, orientador, por=orientador.usuario
        )
```

`aluno.perfil_aluno` (acesso reverso `OneToOneField`, já usado no projeto
inteiro desde o Bloco A) — `_aluno()` devolve o `Usuario`, e
`criar_tcc_ii_manual` espera um `PerfilAluno` (mesmo tipo que
`criar_projeto_sob_limite` já espera, Bloco B).

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k criar_tcc_ii_manual -v`
Expected: `AttributeError` — `criar_tcc_ii_manual` não existe.

- [ ] **Passo 3: Acrescentar `pode_criar_tcc_ii_manual` a `apps/projetos/permissions.py`**

```python
def pode_criar_tcc_ii_manual(usuario, professor):
    """`usuario` é exatamente `professor` (Bloco F, spec §6) — combina o
    portão de papel (`hasattr perfil_professor`, mesmo de `pode_criar_tema`)
    com a posse numa função só: ao contrário de `pode_criar_tema`/
    `pode_criar_tema_para` (duas funções, porque duas telas fazem perguntas
    diferentes), aqui só existe um ponto de entrada — a view sempre passa
    `request.user.perfil_professor` como `professor`."""
    return bool(
        usuario
        and usuario.is_authenticated
        and hasattr(usuario, "perfil_professor")
        and usuario.perfil_professor == professor
    )
```

- [ ] **Passo 4: Acrescentar `criar_tcc_ii_manual` a `apps/projetos/services.py`**

```python
def criar_tcc_ii_manual(aluno, professor, por):
    """Cria um TCC II do zero, sem TCC I anterior no sistema — comprova
    equivalência externa (Bloco F, spec §3.2). Casca fina sobre
    `criar_projeto_sob_limite` (Bloco B, linha 68): reaproveita a checagem
    de vaga testada contra corrida em vez de duplicá-la. `tema=None`: TCC II
    não tem conceito de mural."""
    if not permissions.pode_criar_tcc_ii_manual(por, professor):
        raise PermissionDenied("Somente o próprio professor cria um TCC II em seu nome.")

    return criar_projeto_sob_limite(aluno, professor, tema=None, etapa=Projeto.TCC_II)
```

- [ ] **Passo 5: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k criar_tcc_ii_manual -v`
Expected: todos `PASSED`.

- [ ] **Passo 6: Mutação obrigatória — checagem de posse**

Comente `if not permissions.pode_criar_tcc_ii_manual(por, professor): raise
PermissionDenied(...)` em `criar_tcc_ii_manual` e rode:
Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k recusa_quem_nao_e_o_professor -v`
Expected: `FAILED`. Desfaça, confirme `PASSED` de novo.

- [ ] **Passo 7: Escrever `FormularioCriarTccII` em `apps/projetos/forms.py`**

Ao final do arquivo:

```python
class FormularioCriarTccII(MisturaAcessibilidadeFormulario, forms.Form):
    """Criação manual do TCC II, pra comprovar equivalência externa (Bloco
    F, spec §7). `aluno` lista TODO `PerfilAluno` — sem pré-filtrar quem já
    tem TCC II ativo: `criar_projeto_sob_limite` já recusa com mensagem
    clara via `UniqueConstraint` (`IntegrityError` traduzido, Bloco B),
    então filtrar aqui seria duplicar essa proteção sem necessidade."""

    aluno = forms.ModelChoiceField(
        label="Aluno",
        queryset=PerfilAluno.objects.select_related("usuario").order_by("usuario__nome_completo"),
        widget=forms.Select(attrs={"class": "select w-full"}),
    )
```

Acrescente `PerfilAluno` ao import de `apps.contas.models` no topo do
arquivo (confira a linha exata — hoje importa `Area, PerfilProfessor`).

- [ ] **Passo 8: Escrever a view e a rota**

Em `apps/projetos/views.py`, ao final do arquivo:

```python
@login_required
def criar_tcc_ii_manual_view(request):
    """Professor cria um TCC II manualmente (Bloco F, spec §7). Portão de
    PAPEL primeiro (`pode_criar_tema`, mesmo reaproveitamento de
    `orientacoes`/`meus_temas` — ela só pergunta "é professor?"), antes de
    tocar `perfil_professor`."""
    permissions.garante(
        permissions.pode_criar_tema(request.user), "Somente professores criam TCC II."
    )
    professor = request.user.perfil_professor

    if request.method == "POST":
        formulario = FormularioCriarTccII(request.POST)
        if formulario.is_valid():
            try:
                services.criar_tcc_ii_manual(
                    formulario.cleaned_data["aluno"], professor, por=request.user
                )
            except ValidationError as erro:
                formulario.add_error(None, erro.messages[0])
            else:
                messages.success(request, "TCC II criado.")
                return redirect("projetos:orientacoes")
    else:
        formulario = FormularioCriarTccII()

    return render(request, "projetos/criar_tcc_ii.html", {"formulario": formulario})
```

Acrescente `FormularioCriarTccII` ao import de `apps.projetos.forms` no
topo do arquivo.

Em `apps/projetos/urls.py`:

```python
    path("temas/tcc-ii/criar/", views.criar_tcc_ii_manual_view, name="criar_tcc_ii_manual"),
```

- [ ] **Passo 9: Criar `templates/projetos/criar_tcc_ii.html`**

```html
{% extends "base.html" %}

{% block titulo %} — Criar TCC II{% endblock %}

{% block conteudo %}
  <article class="mx-auto max-w-xl rounded-box bg-base-100 p-6 shadow-sm">
    <h1 class="font-display text-2xl font-bold text-primary">Criar TCC II</h1>
    <p class="mt-2 text-sm text-base-content/80">
      Para alunos com TCC I concluído fora deste sistema (equivalência).
    </p>

    <form method="post" class="mt-6 space-y-4">
      {% csrf_token %}
      {% if formulario.non_field_errors %}
        <div role="alert" class="alert alert-error">
          {% for erro in formulario.non_field_errors %}<p>{{ erro }}</p>{% endfor %}
        </div>
      {% endif %}
      {% for campo in formulario %}
        {% include "contas/_campo.html" with campo=campo %}
      {% endfor %}
      <button type="submit" class="btn btn-primary">Criar</button>
    </form>
  </article>
{% endblock %}
```

- [ ] **Passo 10: Rodar os testes HTTP (escrever, confirmar falha, implementar já feito acima, confirmar passe)**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
@pytest.mark.django_db
def test_criar_tcc_ii_manual_view_redireciona(client):
    orientador = _professor(30, "Orientador Manual View")
    aluno = _aluno(31, "Aluno Manual View")
    client.force_login(orientador.usuario)
    resposta = client.post("/temas/tcc-ii/criar/", {"aluno": aluno.perfil_aluno.pk})
    assert resposta.status_code == 302
    assert Projeto.objects.filter(
        aluno=aluno, etapa=Projeto.TCC_II, orientador=orientador.usuario
    ).exists()
```

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -v`
Expected: todos `PASSED`.

- [ ] **Passo 11: Rodar a suíte de `apps/projetos` inteira**

Run: `docker compose exec web pytest apps/projetos/ -v`
Expected: todos `PASSED`.

- [ ] **Passo 12: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/projetos/services.py apps/projetos/permissions.py apps/projetos/forms.py \
        apps/projetos/views.py apps/projetos/urls.py \
        templates/projetos/criar_tcc_ii.html apps/projetos/tests/test_tcc_ii.py
git commit -m "Tarefa 3: criar_tcc_ii_manual"
```

---

## Tarefa 4: `ItemCorrecao` — serviços, permissões e tela de correções

**Arquivos:**
- Modificar: `apps/bancas/services.py`, `apps/bancas/permissions.py`,
  `apps/bancas/forms.py`, `apps/bancas/views.py`, `apps/bancas/urls.py`.
- Criar: `templates/bancas/correcoes.html`.
- Teste: `apps/bancas/tests/test_correcoes.py` (extensão).

**Interfaces:**
- Produz: `services.criar_item_correcao(projeto, descricao, por) ->
  ItemCorrecao`; `services.concluir_item_correcao(item, por) -> None`;
  `permissions.pode_gerenciar_correcao(usuario, projeto) -> bool`.

- [ ] **Passo 1: Escrever os testes de serviço (falhando)**

Acrescente a `apps/bancas/tests/test_correcoes.py`:

```python
from django.core.exceptions import PermissionDenied

from apps.bancas import permissions, services


@pytest.mark.django_db
def test_criar_item_correcao(projeto_tcc_ii):
    item = services.criar_item_correcao(
        projeto_tcc_ii, descricao="Revisar a bibliografia.", por=projeto_tcc_ii.orientador
    )
    assert item.projeto_id == projeto_tcc_ii.id
    assert item.concluido is False


@pytest.mark.django_db
def test_criar_item_correcao_recusa_quem_nao_e_o_orientador(projeto_tcc_ii):
    from apps.contas.models import Usuario

    outro = Usuario.objects.create_user(
        email="outro.correcao@ufsm.br", password="x", nome_completo="Outro Correção", cpf=_cpf(3)
    )
    with pytest.raises(PermissionDenied):
        services.criar_item_correcao(projeto_tcc_ii, descricao="x", por=outro)


@pytest.mark.django_db
def test_concluir_item_correcao(projeto_tcc_ii):
    item = services.criar_item_correcao(
        projeto_tcc_ii, descricao="Ajustar formatação.", por=projeto_tcc_ii.orientador
    )
    services.concluir_item_correcao(item, por=projeto_tcc_ii.orientador)
    item.refresh_from_db()
    assert item.concluido is True


@pytest.mark.django_db
def test_concluir_item_correcao_recusa_quem_nao_e_o_orientador(projeto_tcc_ii):
    from apps.contas.models import Usuario

    item = services.criar_item_correcao(
        projeto_tcc_ii, descricao="x", por=projeto_tcc_ii.orientador
    )
    outro = Usuario.objects.create_user(
        email="outro.concluir@ufsm.br", password="x", nome_completo="Outro Concluir", cpf=_cpf(4)
    )
    with pytest.raises(PermissionDenied):
        services.concluir_item_correcao(item, por=outro)
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/bancas/tests/test_correcoes.py -k "criar_item or concluir_item" -v`
Expected: `AttributeError` — `permissions`/`services` de correção não existem.

- [ ] **Passo 3: Acrescentar `pode_gerenciar_correcao` a `apps/bancas/permissions.py`**

```python
def pode_gerenciar_correcao(usuario, projeto):
    """`usuario` é exatamente o orientador de `projeto` (Bloco F, spec §6)
    — posse, não papel."""
    return bool(usuario and usuario.is_authenticated and usuario == projeto.orientador)
```

- [ ] **Passo 4: Acrescentar `criar_item_correcao`/`concluir_item_correcao` a `apps/bancas/services.py`**

Ao topo do arquivo, acrescente ao import de `apps.bancas.models`:
`ItemCorrecao`. Ao final do arquivo:

```python
def criar_item_correcao(projeto, descricao, por):
    """Orientador digita um item do checklist de correções (Bloco F, spec
    §5.2) — baseado no que a banca pediu (`Banca.comentario`), não
    estruturado a partir da banca em si. A notificação por e-mail entra na
    Tarefa 7 — esta tarefa entrega só a criação do item."""
    if not permissions.pode_gerenciar_correcao(por, projeto):
        raise PermissionDenied("Somente o orientador do projeto gerencia as correções.")

    return ItemCorrecao.objects.create(projeto=projeto, descricao=descricao)


def concluir_item_correcao(item, por):
    """Orientador marca um item de correção como resolvido (Bloco F, spec
    §5.2). Sem notificação — o aluno já viu o item ao ser criado; concluir
    é informação de controle do orientador, não algo acionável pro aluno."""
    if not permissions.pode_gerenciar_correcao(por, item.projeto):
        raise PermissionDenied("Somente o orientador do projeto gerencia as correções.")

    item.concluido = True
    item.save(update_fields=["concluido"])
```

- [ ] **Passo 5: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/bancas/tests/test_correcoes.py -k "criar_item or concluir_item" -v`
Expected: todos `PASSED`.

- [ ] **Passo 6: Mutação obrigatória — posse de `concluir_item_correcao`**

Comente `if not permissions.pode_gerenciar_correcao(por, item.projeto): raise
PermissionDenied(...)` em `concluir_item_correcao` e rode:
Run: `docker compose exec web pytest apps/bancas/tests/test_correcoes.py -k recusa_quem_nao_e_o_orientador -v`
Expected: pelo menos um `FAILED`. Desfaça, confirme `PASSED` de novo.

- [ ] **Passo 7: `FormularioItemCorrecao` em `apps/bancas/forms.py`**

`apps/bancas/forms.py` já existe desde o Bloco D (`FormularioBanca`,
`FormularioResultadoBanca`), com `from django import forms` e
`from apps.contas.forms import MisturaAcessibilidadeFormulario` já
importados no topo — só acrescente a classe ao final do arquivo:

```python
class FormularioItemCorrecao(MisturaAcessibilidadeFormulario, forms.Form):
    """Criação de um item de correção pelo orientador (Bloco F, spec §7)."""

    descricao = forms.CharField(
        label="Descrição da correção",
        widget=forms.Textarea(attrs={"class": "textarea w-full"}),
    )
```

- [ ] **Passo 8: Views e rotas de `apps/bancas`**

Em `apps/bancas/views.py`, ao final do arquivo:

```python
@login_required
def correcoes(request, projeto_id):
    """Lista os itens de correção do TCC II e permite criar novos (Bloco F,
    spec §7). O botão "Aprovar" desta tela posta pra
    `projetos:aprovar_projeto` (Bloco E) — a mesma rota do TCC_I; o gate da
    Tarefa 6 é quem decide se o pedido é aceito ou recusado."""
    projeto = get_object_or_404(Projeto, pk=projeto_id, orientador=request.user)

    if request.method == "POST":
        formulario = FormularioItemCorrecao(request.POST)
        if formulario.is_valid():
            services.criar_item_correcao(
                projeto, descricao=formulario.cleaned_data["descricao"], por=request.user
            )
            messages.success(request, "Item de correção criado.")
            return redirect("bancas:correcoes", projeto_id=projeto.pk)
    else:
        formulario = FormularioItemCorrecao()

    itens = projeto.itens_correcao.all()
    return render(
        request, "bancas/correcoes.html", {"projeto": projeto, "itens": itens, "formulario": formulario}
    )


@login_required
@require_POST
def concluir_item_view(request, item_id):
    """Marca um item de correção como concluído (Bloco F, spec §7)."""
    item = get_object_or_404(ItemCorrecao, pk=item_id, projeto__orientador=request.user)
    services.concluir_item_correcao(item, por=request.user)
    messages.success(request, "Item marcado como concluído.")
    return redirect("bancas:correcoes", projeto_id=item.projeto_id)
```

`apps/bancas/views.py` já tem `from apps.bancas.forms import
FormularioBanca, FormularioResultadoBanca` e `from apps.bancas.models
import Banca` no topo (Bloco D) — acrescente `ItemCorrecao` à primeira
linha de import e `FormularioItemCorrecao` à segunda.

Em `apps/bancas/urls.py`:

```python
    path("bancas/<int:projeto_id>/correcoes/", views.correcoes, name="correcoes"),
    path(
        "bancas/correcoes/<int:item_id>/concluir/",
        views.concluir_item_view,
        name="concluir_item",
    ),
```

- [ ] **Passo 9: Criar `templates/bancas/correcoes.html`**

```html
{% extends "base.html" %}

{% block titulo %} — Correções{% endblock %}

{% block conteudo %}
  <article class="mx-auto max-w-2xl rounded-box bg-base-100 p-6 shadow-sm">
    <h1 class="font-display text-2xl font-bold text-primary">Correções — {{ projeto.aluno.nome_completo }}</h1>

    {% if itens %}
      <ul class="mt-4 space-y-3">
        {% for item in itens %}
          <li class="rounded-box border border-base-300 p-4">
            <p class="text-sm {% if item.concluido %}line-through text-base-content/60{% endif %}">
              {{ item.descricao }}
            </p>
            {% if not item.concluido %}
              <form method="post" action="{% url 'bancas:concluir_item' item.pk %}" class="mt-2">
                {% csrf_token %}
                <button type="submit" class="btn btn-outline btn-sm">Marcar como concluído</button>
              </form>
            {% else %}
              <span class="badge badge-success">Concluído</span>
            {% endif %}
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p class="mt-4 text-base-content/80">Nenhum item de correção cadastrado ainda.</p>
    {% endif %}

    <form method="post" class="mt-6 space-y-4">
      {% csrf_token %}
      {% for campo in formulario %}
        {% include "contas/_campo.html" with campo=campo %}
      {% endfor %}
      <button type="submit" class="btn btn-outline">Adicionar item</button>
    </form>

    <form method="post" action="{% url 'projetos:aprovar_projeto' projeto.pk %}" class="mt-6">
      {% csrf_token %}
      <button type="submit" class="btn btn-primary">Aprovar projeto</button>
    </form>
  </article>
{% endblock %}
```

- [ ] **Passo 10: Escrever os testes HTTP**

Acrescente a `apps/bancas/tests/test_correcoes.py`:

```python
@pytest.mark.django_db
def test_correcoes_view_lista_itens(client, projeto_tcc_ii):
    services.criar_item_correcao(
        projeto_tcc_ii, descricao="Item de teste da tela.", por=projeto_tcc_ii.orientador
    )
    client.force_login(projeto_tcc_ii.orientador)
    resposta = client.get(f"/bancas/{projeto_tcc_ii.pk}/correcoes/")
    assert "Item de teste da tela." in resposta.content.decode()


@pytest.mark.django_db
def test_correcoes_view_recusa_projeto_alheio_com_404(client, projeto_tcc_ii):
    from apps.contas.models import PerfilProfessor, Usuario

    outro = Usuario.objects.create_user(
        email="outro.correcoesview@ufsm.br",
        password="x",
        nome_completo="Outro Correções View",
        cpf=_cpf(5),
    )
    PerfilProfessor.objects.create(usuario=outro, siape="CORRVIEW1")
    client.force_login(outro)
    resposta = client.get(f"/bancas/{projeto_tcc_ii.pk}/correcoes/")
    assert resposta.status_code == 404


@pytest.mark.django_db
def test_concluir_item_view_redireciona(client, projeto_tcc_ii):
    item = services.criar_item_correcao(
        projeto_tcc_ii, descricao="Item pra concluir via view.", por=projeto_tcc_ii.orientador
    )
    client.force_login(projeto_tcc_ii.orientador)
    resposta = client.post(f"/bancas/correcoes/{item.pk}/concluir/")
    assert resposta.status_code == 302
    item.refresh_from_db()
    assert item.concluido is True
```

Run: `docker compose exec web pytest apps/bancas/tests/test_correcoes.py -v`
Expected: todos `PASSED`.

- [ ] **Passo 11: Rodar a suíte de `apps/bancas` inteira**

Run: `docker compose exec web pytest apps/bancas/ -v`
Expected: todos `PASSED`.

- [ ] **Passo 12: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/bancas/services.py apps/bancas/permissions.py apps/bancas/forms.py \
        apps/bancas/views.py apps/bancas/urls.py templates/bancas/correcoes.html \
        apps/bancas/tests/test_correcoes.py
git commit -m "Tarefa 4: ItemCorrecao (servicos, permissoes, tela de correcoes)"
```

---

## Tarefa 5: `TermoPublicacao` e extensão de `/meu-tcc/`

**Arquivos:**
- Modificar: `apps/projetos/services.py`, `apps/projetos/permissions.py`,
  `apps/projetos/views.py`, `templates/projetos/meu_tcc.html`.
- Teste: `apps/projetos/tests/test_tcc_ii.py` (extensão),
  `apps/projetos/tests/test_tela_meu_tcc.py` (extensão).

**Interfaces:**
- Produz: `services.assinar_termo_publicacao(projeto, por) ->
  TermoPublicacao`; `permissions.pode_assinar_termo(usuario, projeto) -> bool`.
- Modifica: `views.py::meu_tcc` — tenta `Projeto.TCC_II` antes de
  `Projeto.TCC_I`.

- [ ] **Passo 1: Escrever os testes de serviço (falhando)**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
@pytest.mark.django_db
def test_assinar_termo_publicacao_cria_a_linha():
    orientador = _professor(40, "Orientador Termo")
    aluno = _aluno(41, "Aluno Termo")
    projeto = Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=1,
    )
    termo = services.assinar_termo_publicacao(projeto, por=aluno)
    assert termo.projeto_id == projeto.id


@pytest.mark.django_db
def test_assinar_termo_publicacao_recusa_quem_nao_e_o_aluno():
    orientador = _professor(42, "Orientador Termo Dois")
    aluno = _aluno(43, "Aluno Termo Dois")
    outro = _aluno(44, "Outro Aluno Termo")
    projeto = Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=1,
    )
    with pytest.raises(PermissionDenied):
        services.assinar_termo_publicacao(projeto, por=outro)
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k assinar_termo -v`
Expected: `AttributeError` — `assinar_termo_publicacao` não existe.

- [ ] **Passo 3: Acrescentar `pode_assinar_termo` a `apps/projetos/permissions.py`**

```python
def pode_assinar_termo(usuario, projeto):
    """`usuario` é exatamente o aluno de `projeto` (Bloco F, spec §6) —
    posse, mesmo padrão de `pode_enviar_submissao` (Bloco C)."""
    return bool(usuario and usuario.is_authenticated and usuario == projeto.aluno)
```

- [ ] **Passo 4: Acrescentar `assinar_termo_publicacao` a `apps/projetos/services.py`**

Acrescente `TermoPublicacao` ao import de `apps.projetos.models` no topo do
arquivo. Ao final do arquivo:

```python
def assinar_termo_publicacao(projeto, por):
    """Aluno assina o aceite de publicação do TCC II (Bloco F, spec §3.4) —
    a existência da linha já significa "assinado"; sem estado intermediário,
    sem como desfazer."""
    if not permissions.pode_assinar_termo(por, projeto):
        raise PermissionDenied("Somente o aluno do projeto assina o termo.")

    return TermoPublicacao.objects.create(projeto=projeto)
```

- [ ] **Passo 5: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k assinar_termo -v`
Expected: todos `PASSED`.

- [ ] **Passo 6: Mutação obrigatória — posse de `assinar_termo_publicacao`**

Comente a checagem de posse dentro de `assinar_termo_publicacao` e rode:
Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k recusa_quem_nao_e_o_aluno -v`
Expected: `FAILED`. Desfaça, confirme `PASSED` de novo.

- [ ] **Passo 7: Estender a view `meu_tcc`**

Leia `apps/projetos/views.py::meu_tcc` por completo antes de editar (este
spec foi escrito antes de qualquer edição do Bloco F nela — confira a linha
exata). Troque a linha:

```python
    projeto = services.projeto_ativo_do_aluno(request.user, Projeto.TCC_I)
```

por:

```python
    projeto = services.projeto_ativo_do_aluno(
        request.user, Projeto.TCC_II
    ) or services.projeto_ativo_do_aluno(request.user, Projeto.TCC_I)
```

Depois do bloco que monta `submissao_atual`/processa o `POST` de envio de
arquivo (que continua igual, reaproveitado sem mudança para TCC_II), mas
antes do `return render(...)` final, acrescente o processamento do POST de
assinatura do termo — como agora há DOIS formulários possíveis na mesma
tela (envio de arquivo e assinatura de termo), distinga por um campo oculto
no HTML (`name="acao"`):

```python
    itens_correcao_pendentes = []
    pode_assinar = False
    ja_assinou = False
    if projeto is not None and projeto.etapa == Projeto.TCC_II:
        itens_correcao_pendentes = projeto.itens_correcao.filter(concluido=False)
        ja_assinou = hasattr(projeto, "termo_publicacao")
        pode_assinar = projeto.status == Projeto.APROVADO_COM_RESSALVAS and not ja_assinou

    if request.method == "POST" and request.POST.get("acao") == "assinar_termo":
        services.assinar_termo_publicacao(projeto, por=request.user)
        messages.success(request, "Termo de publicação assinado.")
        return redirect("projetos:meu_tcc")
```

Este bloco entra ANTES do `if request.method == "POST":` que já trata o
envio de `FormularioSubmissao` — confira a ordem real ao editar: o
`acao == "assinar_termo"` precisa ser checado primeiro, para não cair no
`FormularioSubmissao(request.POST, request.FILES)` por engano (que erraria
ao não achar os campos de arquivo).

Acrescente `itens_correcao_pendentes`, `pode_assinar`, `ja_assinou` ao
dicionário de contexto do `render` final.

- [ ] **Passo 8: Estender `templates/projetos/meu_tcc.html`**

Leia o arquivo por completo antes de editar. Acrescente, dentro do bloco
que já mostra `projeto`/formulário de envio (depois do formulário de
`FormularioSubmissao`, antes do `{% endif %}` que fecha o "tem projeto
ativo"):

```html
              {% if projeto.etapa == "TCC_II" %}
                {% if itens_correcao_pendentes %}
                  <section class="mt-6">
                    <h2 class="font-display text-lg font-bold text-primary">Correções pendentes</h2>
                    <ul class="mt-2 space-y-2">
                      {% for item in itens_correcao_pendentes %}
                        <li class="text-sm">{{ item.descricao }}</li>
                      {% endfor %}
                    </ul>
                  </section>
                {% endif %}
                {% if pode_assinar %}
                  <form method="post" class="mt-6">
                    {% csrf_token %}
                    <input type="hidden" name="acao" value="assinar_termo">
                    <button type="submit" class="btn btn-primary">
                      Assinar termo de aceite de publicação
                    </button>
                  </form>
                {% elif ja_assinou %}
                  <p class="mt-4 text-sm text-base-content/70">Termo de publicação já assinado.</p>
                {% endif %}
              {% endif %}
```

- [ ] **Passo 9: Escrever os testes HTTP**

Acrescente a `apps/projetos/tests/test_tela_meu_tcc.py` (arquivo já
existente, Bloco C) — com suas próprias fábricas: `_cpf_valido(base_int)`
(faixa `950000010`-`950000012` já ocupada pelas fixtures
`aluno`/`professor`/`sem_perfil` do arquivo — os dois testes abaixo usam
`950000020`+, livre), e fixtures `aluno`/`professor` que são `Usuario`
simples (não `PerfilAluno`/`PerfilProfessor`). O `projeto` fixture do
arquivo usa `Projeto.TCC_I`; os dois testes novos criam seu próprio
`Projeto` `TCC_II`, sem reaproveitar essa fixture:

```python
@pytest.mark.django_db
def test_meu_tcc_mostra_correcoes_pendentes_e_botao_de_termo(client):
    from apps.bancas.models import ItemCorrecao

    aluno_usuario = Usuario.objects.create_user(
        email="aluno.meutccii.correcoes@ufsm.br",
        password="x",
        nome_completo="Aluno Meu TCC II Correções",
        papel=Usuario.ALUNO,
        cpf=_cpf_valido(950000020),
    )
    PerfilAluno.objects.create(usuario=aluno_usuario, matricula="2026MTC0020")
    professor_usuario = Usuario.objects.create_user(
        email="professor.meutccii.correcoes@ufsm.br",
        password="x",
        nome_completo="Professor Meu TCC II Correções",
        cpf=_cpf_valido(950000021),
    )
    PerfilProfessor.objects.create(usuario=professor_usuario, siape="9500021")
    projeto_tcc_ii = Projeto.objects.create(
        aluno=aluno_usuario,
        orientador=professor_usuario,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=2,
    )
    ItemCorrecao.objects.create(projeto=projeto_tcc_ii, descricao="Ajustar a introdução.")

    client.force_login(aluno_usuario)
    resposta = client.get(reverse("projetos:meu_tcc"))
    conteudo = resposta.content.decode()
    assert "Ajustar a introdução." in conteudo
    assert "Assinar termo de aceite de publicação" in conteudo


@pytest.mark.django_db
def test_meu_tcc_assinar_termo_cria_a_linha(client):
    from apps.projetos.models import TermoPublicacao

    aluno_usuario = Usuario.objects.create_user(
        email="aluno.meutccii.termo@ufsm.br",
        password="x",
        nome_completo="Aluno Meu TCC II Termo",
        papel=Usuario.ALUNO,
        cpf=_cpf_valido(950000022),
    )
    PerfilAluno.objects.create(usuario=aluno_usuario, matricula="2026MTC0022")
    professor_usuario = Usuario.objects.create_user(
        email="professor.meutccii.termo@ufsm.br",
        password="x",
        nome_completo="Professor Meu TCC II Termo",
        cpf=_cpf_valido(950000023),
    )
    PerfilProfessor.objects.create(usuario=professor_usuario, siape="9500023")
    projeto_tcc_ii = Projeto.objects.create(
        aluno=aluno_usuario,
        orientador=professor_usuario,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=2,
    )

    client.force_login(aluno_usuario)
    resposta = client.post(reverse("projetos:meu_tcc"), {"acao": "assinar_termo"})
    assert resposta.status_code == 302
    assert TermoPublicacao.objects.filter(projeto=projeto_tcc_ii).exists()
```

Acrescente `PerfilAluno`, `PerfilProfessor` ao import de
`apps.contas.models` no topo do arquivo (hoje já importa os dois — confira
antes de duplicar).

- [ ] **Passo 10: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tela_meu_tcc.py apps/projetos/tests/test_tcc_ii.py -v`
Expected: todos `PASSED`.

- [ ] **Passo 11: Rodar a suíte de `apps/projetos` inteira**

Run: `docker compose exec web pytest apps/projetos/ -v`
Expected: todos `PASSED` — nenhuma regressão nos testes do Bloco C sobre
`/meu-tcc/` mostrando TCC_I (agora o fallback, não o caminho único).

- [ ] **Passo 12: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/projetos/services.py apps/projetos/permissions.py apps/projetos/views.py \
        templates/projetos/meu_tcc.html apps/projetos/tests/test_tcc_ii.py \
        apps/projetos/tests/test_tela_meu_tcc.py
git commit -m "Tarefa 5: assinar_termo_publicacao e /meu-tcc/ reconhece TCC_II"
```

---

## Tarefa 6: Gate de `aprovar_projeto` para TCC_II

**Arquivos:**
- Modificar: `apps/projetos/services.py`, `apps/projetos/views.py`.
- Teste: `apps/projetos/tests/test_tcc_ii.py` (extensão),
  `apps/projetos/tests/test_aprovacao.py` (extensão, para a correção do
  achado real).

**Interfaces:**
- Modifica: `aprovar_projeto(projeto, por)` — mesma assinatura, ganha
  checagens condicionais a `etapa == TCC_II`.

- [ ] **Passo 1: Escrever os testes (falhando)**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
from apps.bancas import services as bancas_services


@pytest.fixture
def projeto_tcc_ii_com_ressalvas(db):
    orientador = _professor(60, "Orientador Gate")
    aluno = _aluno(61, "Aluno Gate")
    return Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=1,
    )


@pytest.mark.django_db
def test_aprovar_projeto_tcc_ii_recusa_com_item_pendente(projeto_tcc_ii_com_ressalvas):
    bancas_services.criar_item_correcao(
        projeto_tcc_ii_com_ressalvas,
        descricao="Pendente.",
        por=projeto_tcc_ii_com_ressalvas.orientador,
    )
    services.assinar_termo_publicacao(
        projeto_tcc_ii_com_ressalvas, por=projeto_tcc_ii_com_ressalvas.aluno
    )
    with pytest.raises(ValidationError):
        services.aprovar_projeto(
            projeto_tcc_ii_com_ressalvas, por=projeto_tcc_ii_com_ressalvas.orientador
        )


@pytest.mark.django_db
def test_aprovar_projeto_tcc_ii_recusa_sem_termo(projeto_tcc_ii_com_ressalvas):
    item = bancas_services.criar_item_correcao(
        projeto_tcc_ii_com_ressalvas,
        descricao="Vai ser concluído.",
        por=projeto_tcc_ii_com_ressalvas.orientador,
    )
    bancas_services.concluir_item_correcao(item, por=projeto_tcc_ii_com_ressalvas.orientador)
    with pytest.raises(ValidationError):
        services.aprovar_projeto(
            projeto_tcc_ii_com_ressalvas, por=projeto_tcc_ii_com_ressalvas.orientador
        )


@pytest.mark.django_db
def test_aprovar_projeto_tcc_ii_aprova_com_tudo_pronto(projeto_tcc_ii_com_ressalvas):
    item = bancas_services.criar_item_correcao(
        projeto_tcc_ii_com_ressalvas,
        descricao="Vai ser concluído.",
        por=projeto_tcc_ii_com_ressalvas.orientador,
    )
    bancas_services.concluir_item_correcao(item, por=projeto_tcc_ii_com_ressalvas.orientador)
    services.assinar_termo_publicacao(
        projeto_tcc_ii_com_ressalvas, por=projeto_tcc_ii_com_ressalvas.aluno
    )
    services.aprovar_projeto(
        projeto_tcc_ii_com_ressalvas, por=projeto_tcc_ii_com_ressalvas.orientador
    )
    projeto_tcc_ii_com_ressalvas.refresh_from_db()
    assert projeto_tcc_ii_com_ressalvas.status == Projeto.APROVADO


@pytest.mark.django_db
def test_aprovar_projeto_tcc_i_continua_sem_gate(orientador, projeto_com_ressalvas):
    """Confirma que o TCC_I NÃO é afetado pelo gate novo — mesmo teste do
    Bloco E, reafirmado aqui de propósito (checagem vizinha pode mascarar
    regressão)."""
    services.aprovar_projeto(projeto_com_ressalvas, por=orientador.usuario)
    projeto_com_ressalvas.refresh_from_db()
    assert projeto_com_ressalvas.status == Projeto.APROVADO
```

(`orientador`/`projeto_com_ressalvas` são as fixtures já definidas em
`apps/projetos/tests/test_aprovacao.py` — como `test_tcc_ii.py` é um
arquivo DIFERENTE, essas fixtures não existem aqui automaticamente; troque
o último teste acima para viver em `test_aprovacao.py`, não em
`test_tcc_ii.py` — acrescente-o lá, ao final do arquivo, reaproveitando as
fixtures já existentes.)

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k "aprovar_projeto_tcc_ii" -v`
Expected: os dois testes de recusa FALHAM (nada impede `aprovar_projeto`
hoje); o de "aprova com tudo pronto" passa à toa.

- [ ] **Passo 3: Acrescentar o gate a `aprovar_projeto`**

Em `apps/projetos/services.py`, dentro de `aprovar_projeto`, logo depois da
checagem `if projeto.status != Projeto.APROVADO_COM_RESSALVAS: raise
ValidationError(...)`:

```python
    if projeto.etapa == Projeto.TCC_II:
        if projeto.itens_correcao.filter(concluido=False).exists():
            raise ValidationError(
                "Ainda há itens de correção pendentes — conclua todos antes de aprovar."
            )
        if not hasattr(projeto, "termo_publicacao"):
            raise ValidationError(
                "O aluno ainda não assinou o termo de aceite de publicação."
            )
```

- [ ] **Passo 4: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py apps/projetos/tests/test_aprovacao.py -v`
Expected: todos `PASSED`.

- [ ] **Passo 5: Mutação obrigatória — as duas condições do gate, separadamente**

Comente só a checagem de `itens_correcao` (deixe a de `termo_publicacao`) e
rode:
Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k recusa_com_item_pendente -v`
Expected: `FAILED`. Desfaça.

Comente só a checagem de `termo_publicacao` (deixe a de `itens_correcao`) e
rode:
Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k recusa_sem_termo -v`
Expected: `FAILED` — prova que uma checagem não mascara a ausência da outra
(CLAUDE.md, "checagem vizinha"). Desfaça, confirme os dois `PASSED` de novo.

- [ ] **Passo 6: Corrigir `aprovar_projeto_view` (achado real, latente desde o Bloco E)**

`apps/projetos/views.py::aprovar_projeto_view` hoje chama
`services.aprovar_projeto(projeto, por=request.user)` sem capturar
`ValidationError` — inofensivo enquanto o único jeito de chegar lá era o
botão condicional do Bloco E (só aparece com o status certo), mas o gate
desta tarefa torna a falha alcançável em uso normal (correções pendentes,
termo não assinado) a partir da tela de correções (Tarefa 4). Sem captura,
isso seria um 500. Troque:

```python
    projeto = get_object_or_404(Projeto, pk=projeto_id, orientador=request.user)
    services.aprovar_projeto(projeto, por=request.user)
    messages.success(request, "Projeto aprovado. A ata foi gerada e enviada à SUGRAD.")
    return redirect("projetos:orientacoes")
```

por:

```python
    projeto = get_object_or_404(Projeto, pk=projeto_id, orientador=request.user)
    try:
        services.aprovar_projeto(projeto, por=request.user)
    except ValidationError as erro:
        messages.error(request, erro.messages[0])
    else:
        messages.success(request, "Projeto aprovado. A ata foi gerada e enviada à SUGRAD.")
    return redirect("projetos:orientacoes")
```

- [ ] **Passo 7: Escrever o teste HTTP da correção**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
@pytest.mark.django_db
def test_aprovar_projeto_view_com_item_pendente_nao_da_500(client, projeto_tcc_ii_com_ressalvas):
    bancas_services.criar_item_correcao(
        projeto_tcc_ii_com_ressalvas,
        descricao="Pendente.",
        por=projeto_tcc_ii_com_ressalvas.orientador,
    )
    services.assinar_termo_publicacao(
        projeto_tcc_ii_com_ressalvas, por=projeto_tcc_ii_com_ressalvas.aluno
    )
    client.force_login(projeto_tcc_ii_com_ressalvas.orientador)
    resposta = client.post(f"/orientacoes/{projeto_tcc_ii_com_ressalvas.pk}/aprovar/")
    assert resposta.status_code == 302
    projeto_tcc_ii_com_ressalvas.refresh_from_db()
    assert projeto_tcc_ii_com_ressalvas.status == Projeto.APROVADO_COM_RESSALVAS
```

- [ ] **Passo 8: Rodar e confirmar que passa; mutação da correção**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k nao_da_500 -v`
Expected: `PASSED`. Desfaça o `try/except` do Passo 6 temporariamente
(volte à chamada direta) e rode de novo:
Expected: erro não tratado (`ValidationError` propaga —
pytest reporta como erro do teste, não `FAILED` limpo, mas confirma que
sem o `try/except` o teste não passaria da mesma forma). Restaure o
`try/except`.

- [ ] **Passo 9: Rodar a suíte de `apps/projetos` inteira**

Run: `docker compose exec web pytest apps/projetos/ -v`
Expected: todos `PASSED`.

- [ ] **Passo 10: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/projetos/services.py apps/projetos/views.py \
        apps/projetos/tests/test_tcc_ii.py apps/projetos/tests/test_aprovacao.py
git commit -m "Tarefa 6: gate de aprovar_projeto para TCC_II"
```

---

## Tarefa 7: Notificações

**Arquivos:**
- Modificar: `apps/projetos/tasks.py`, `apps/bancas/services.py`,
  `apps/bancas/tasks.py` (criar se não existir), `apps/projetos/services.py`.
- Criar: `templates/email/tcc_ii_criado.txt`,
  `templates/email/item_correcao_criado.txt`.
- Teste: `apps/projetos/tests/test_tcc_ii.py` (extensão),
  `apps/bancas/tests/test_correcoes.py` (extensão).

**Interfaces:**
- Produz: `apps.projetos.tasks.enviar_tcc_ii_criado(projeto_id)`;
  `apps.bancas.tasks.enviar_item_correcao_criado(item_id)`.

- [ ] **Passo 1: Escrever os testes (falhando)**

Acrescente a `apps/projetos/tests/test_tcc_ii.py`:

```python
@pytest.mark.django_db
def test_criar_tcc_ii_automatico_notifica_aluno(
    settings, django_capture_on_commit_callbacks, projeto_tcc_i
):
    from django.core import mail

    settings.CELERY_TASK_ALWAYS_EAGER = True
    with django_capture_on_commit_callbacks(execute=True):
        services.criar_tcc_ii_automatico(projeto_tcc_i)
    assert len(mail.outbox) == 1
    assert mail.outbox[0].to == [projeto_tcc_i.aluno.email]


@pytest.mark.django_db
def test_criar_tcc_ii_manual_notifica_aluno(settings, django_capture_on_commit_callbacks):
    from django.core import mail

    orientador = _professor(70, "Orientador Notif Manual")
    aluno = _aluno(71, "Aluno Notif Manual")
    settings.CELERY_TASK_ALWAYS_EAGER = True
    with django_capture_on_commit_callbacks(execute=True):
        services.criar_tcc_ii_manual(aluno.perfil_aluno, orientador, por=orientador.usuario)
    assert len(mail.outbox) == 1
    assert mail.outbox[0].to == [aluno.email]
```

Acrescente a `apps/bancas/tests/test_correcoes.py`:

```python
@pytest.mark.django_db
def test_criar_item_correcao_notifica_aluno(
    settings, django_capture_on_commit_callbacks, projeto_tcc_ii
):
    from django.core import mail

    settings.CELERY_TASK_ALWAYS_EAGER = True
    with django_capture_on_commit_callbacks(execute=True):
        services.criar_item_correcao(
            projeto_tcc_ii, descricao="Ajustar.", por=projeto_tcc_ii.orientador
        )
    assert len(mail.outbox) == 1
    assert mail.outbox[0].to == [projeto_tcc_ii.aluno.email]
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k notifica -v apps/bancas/tests/test_correcoes.py -k notifica`
Expected: `mail.outbox` vazio nos três — nada dispara e-mail ainda.

- [ ] **Passo 3: Criar os templates de e-mail**

`templates/email/tcc_ii_criado.txt`:

```
Olá, {{ aluno.nome_completo }}.

Seu TCC II foi criado no sistema, orientado por {{ orientador.nome_completo }}.

Entre no sistema para enviar seu trabalho: {{ link }}

OrientaSI — Sistema de Gestão de TCC
```

`templates/email/item_correcao_criado.txt`:

```
Olá, {{ aluno.nome_completo }}.

Seu orientador registrou um novo item de correção no seu TCC II:

{{ item.descricao }}

Entre no sistema para ver todos os itens pendentes: {{ link }}

OrientaSI — Sistema de Gestão de TCC
```

- [ ] **Passo 4: Acrescentar `enviar_tcc_ii_criado` a `apps/projetos/tasks.py`**

Ao final do arquivo (reaproveita `_link_login`, já definida no topo do
arquivo desde o Bloco B):

```python
@shared_task(bind=True, max_retries=3)
def enviar_tcc_ii_criado(self, projeto_id):
    """Avisa o aluno de que o TCC II foi criado — automático ou manual
    (Bloco F, spec §8)."""
    from apps.projetos.models import Projeto

    projeto = Projeto.objects.select_related("aluno", "orientador").get(pk=projeto_id)
    corpo = render_to_string(
        "email/tcc_ii_criado.txt",
        {"aluno": projeto.aluno, "orientador": projeto.orientador, "link": _link_login()},
    )
    try:
        send_mail(
            subject="OrientaSI — seu TCC II foi criado",
            message=corpo,
            from_email=settings.DEFAULT_FROM_EMAIL,
            recipient_list=[projeto.aluno.email],
        )
    except Exception as erro:  # noqa: BLE001 — repetimos qualquer falha de entrega
        raise self.retry(exc=erro, countdown=60 * 2**self.request.retries) from erro
```

- [ ] **Passo 5: Disparar `enviar_tcc_ii_criado` nos dois pontos de criação**

Em `apps/projetos/services.py`, troque o corpo de `criar_tcc_ii_automatico`
(Tarefa 2) de:

```python
    ano, periodo = semestre_vigente()
    return Projeto.objects.create(
        aluno=projeto_tcc_i.aluno,
        orientador=projeto_tcc_i.orientador,
        coorientador=projeto_tcc_i.coorientador,
        coorientador_externo=projeto_tcc_i.coorientador_externo,
        etapa=Projeto.TCC_II,
        status=Projeto.EM_ANDAMENTO,
        anterior=projeto_tcc_i,
        ano=ano,
        periodo=periodo,
    )
```

para:

```python
    ano, periodo = semestre_vigente()
    tcc_ii = Projeto.objects.create(
        aluno=projeto_tcc_i.aluno,
        orientador=projeto_tcc_i.orientador,
        coorientador=projeto_tcc_i.coorientador,
        coorientador_externo=projeto_tcc_i.coorientador_externo,
        etapa=Projeto.TCC_II,
        status=Projeto.EM_ANDAMENTO,
        anterior=projeto_tcc_i,
        ano=ano,
        periodo=periodo,
    )

    from apps.projetos.tasks import enviar_tcc_ii_criado

    transaction.on_commit(lambda: enviar_tcc_ii_criado.delay(tcc_ii.id))

    return tcc_ii
```

E troque o corpo de `criar_tcc_ii_manual` (Tarefa 3) de:

```python
    if not permissions.pode_criar_tcc_ii_manual(por, professor):
        raise PermissionDenied("Somente o próprio professor cria um TCC II em seu nome.")

    return criar_projeto_sob_limite(aluno, professor, tema=None, etapa=Projeto.TCC_II)
```

para:

```python
    if not permissions.pode_criar_tcc_ii_manual(por, professor):
        raise PermissionDenied("Somente o próprio professor cria um TCC II em seu nome.")

    tcc_ii = criar_projeto_sob_limite(aluno, professor, tema=None, etapa=Projeto.TCC_II)

    from apps.projetos.tasks import enviar_tcc_ii_criado

    transaction.on_commit(lambda: enviar_tcc_ii_criado.delay(tcc_ii.id))

    return tcc_ii
```

- [ ] **Passo 6: Criar/estender `apps/bancas/tasks.py` com `enviar_item_correcao_criado`**

`apps/bancas/tasks.py` já existe desde o Bloco D (`enviar_agendamento_banca`)
— acrescente ao final:

```python
@shared_task(bind=True, max_retries=3)
def enviar_item_correcao_criado(self, item_id):
    """Avisa o aluno de um novo item de correção — um e-mail por item,
    sem agrupar (Bloco F, spec §8, decisão confirmada no brainstorming)."""
    from apps.bancas.models import ItemCorrecao

    item = ItemCorrecao.objects.select_related("projeto__aluno").get(pk=item_id)
    corpo = render_to_string(
        "email/item_correcao_criado.txt",
        {"aluno": item.projeto.aluno, "item": item, "link": _link_login()},
    )
    try:
        send_mail(
            subject="OrientaSI — novo item de correção no seu TCC II",
            message=corpo,
            from_email=settings.DEFAULT_FROM_EMAIL,
            recipient_list=[item.projeto.aluno.email],
        )
    except Exception as erro:  # noqa: BLE001 — repetimos qualquer falha de entrega
        raise self.retry(exc=erro, countdown=60 * 2**self.request.retries) from erro
```

- [ ] **Passo 7: Disparar a notificação em `criar_item_correcao`**

Em `apps/bancas/services.py`, ao topo do arquivo, acrescente:

```python
from django.db import transaction

from apps.bancas.tasks import enviar_item_correcao_criado
```

E, dentro de `criar_item_correcao` (Tarefa 4), troque:

```python
    return ItemCorrecao.objects.create(projeto=projeto, descricao=descricao)
```

por:

```python
    item = ItemCorrecao.objects.create(projeto=projeto, descricao=descricao)

    transaction.on_commit(lambda: enviar_item_correcao_criado.delay(item.id))

    return item
```

- [ ] **Passo 8: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_tcc_ii.py -k notifica -v`
Run: `docker compose exec web pytest apps/bancas/tests/test_correcoes.py -k notifica -v`
Expected: todos `PASSED`.

- [ ] **Passo 9: Rodar as duas suítes inteiras**

Run: `docker compose exec web pytest apps/projetos/ apps/bancas/ apps/documentos/ -v`
Expected: todos `PASSED`.

- [ ] **Passo 10: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add apps/projetos/tasks.py apps/bancas/tasks.py apps/projetos/services.py \
        apps/bancas/services.py templates/email/tcc_ii_criado.txt \
        templates/email/item_correcao_criado.txt \
        apps/projetos/tests/test_tcc_ii.py apps/bancas/tests/test_correcoes.py
git commit -m "Tarefa 7: notificacoes de TCC II criado e de item de correcao"
```

---

## Tarefa 8: Extensão de `/orientacoes/` (coorientador + correções)

**Arquivos:**
- Modificar: `templates/projetos/orientacoes.html`.
- Teste: `apps/projetos/tests/test_orientacoes_atas.py` (extensão, ou novo
  `apps/projetos/tests/test_orientacoes_tcc_ii.py`).

**Interfaces:**
- Consome: `projeto.etapa`, `projeto.coorientador`,
  `projeto.coorientador_externo` (Tarefa 1).

- [ ] **Passo 1: Escrever os testes (falhando)**

Crie `apps/projetos/tests/test_orientacoes_tcc_ii.py`:

```python
"""Testes de `/orientacoes/` para o Bloco F: exibição de coorientador, e
"Gerenciar correções" (não "Aprovar" direto) para TCC_II em
Aprovado com Ressalvas."""

import pytest

from apps.contas.models import PerfilAluno, PerfilProfessor, Usuario
from apps.contas.validators import _digito
from apps.projetos.models import Projeto


def _cpf(indice):
    base = f"{830000000 + indice:09d}"
    d1 = _digito(base, 10)
    d2 = _digito(base + str(d1), 11)
    return base + str(d1) + str(d2)


def _professor(indice, nome):
    usuario = Usuario.objects.create_user(
        email=f"professor.orientacoestccii.{indice}@ufsm.br",
        password="x",
        nome_completo=nome,
        cpf=_cpf(indice),
    )
    return PerfilProfessor.objects.create(usuario=usuario, siape=f"ORITCCII{indice:02d}")


def _aluno(indice, nome):
    usuario = Usuario.objects.create_user(
        email=f"aluno.orientacoestccii.{indice}@ufsm.br",
        password="x",
        nome_completo=nome,
        papel=Usuario.ALUNO,
        cpf=_cpf(indice),
    )
    PerfilAluno.objects.create(usuario=usuario, matricula=f"2026ORITCCII{indice:02d}")
    return usuario


@pytest.mark.django_db
def test_orientacoes_mostra_gerenciar_correcoes_para_tcc_ii(client):
    orientador = _professor(1, "Orientador TCC II Link")
    aluno = _aluno(2, "Aluno TCC II Link")
    projeto = Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=1,
    )
    client.force_login(orientador.usuario)
    conteudo = client.get("/orientacoes/").content.decode()
    assert f"/bancas/{projeto.pk}/correcoes/" in conteudo
    assert f"/orientacoes/{projeto.pk}/aprovar/" not in conteudo


@pytest.mark.django_db
def test_orientacoes_continua_mostrando_aprovar_direto_para_tcc_i(client):
    orientador = _professor(3, "Orientador TCC I Link")
    aluno = _aluno(4, "Aluno TCC I Link")
    projeto = Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        etapa=Projeto.TCC_I,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=2026,
        periodo=1,
    )
    client.force_login(orientador.usuario)
    conteudo = client.get("/orientacoes/").content.decode()
    assert f"/orientacoes/{projeto.pk}/aprovar/" in conteudo


@pytest.mark.django_db
def test_orientacoes_mostra_coorientador_interno(client):
    orientador = _professor(5, "Orientador Coorientador Link")
    coorientador = _professor(6, "Coorientador Link")
    aluno = _aluno(7, "Aluno Coorientador Link")
    Projeto.objects.create(
        aluno=aluno,
        orientador=orientador.usuario,
        coorientador=coorientador,
        etapa=Projeto.TCC_I,
        status=Projeto.EM_ANDAMENTO,
        ano=2026,
        periodo=1,
    )
    client.force_login(orientador.usuario)
    conteudo = client.get("/orientacoes/").content.decode()
    assert "Coorientador Link" in conteudo
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `docker compose exec web pytest apps/projetos/tests/test_orientacoes_tcc_ii.py -v`
Expected: falhas nos três — nada disso existe no template ainda (o segundo
teste, "continua mostrando aprovar direto para TCC_I", pode passar à toa se
a lógica atual já for correta — confirme ao rodar).

- [ ] **Passo 3: Estender o parágrafo de etapa/tema com o coorientador**

Em `templates/projetos/orientacoes.html`, logo depois do parágrafo:

```html
              <p class="text-sm text-base-content/80">
                {{ projeto.get_etapa_display }}
                {% if projeto.tema %} — {{ projeto.tema.titulo }}{% endif %}
              </p>
```

acrescente:

```html
              {% if projeto.coorientador or projeto.coorientador_externo %}
                <p class="text-sm text-base-content/80">
                  Coorientador:
                  {% if projeto.coorientador %}{{ projeto.coorientador.usuario.nome_completo }}{% else %}{{ projeto.coorientador_externo }} (externo){% endif %}
                </p>
              {% endif %}
```

- [ ] **Passo 4: Trocar o ramo `APROVADO_COM_RESSALVAS` para checar a etapa**

Troque:

```html
              {% elif projeto.status == "APROVADO_COM_RESSALVAS" %}
                <div class="mt-2">
                  <form method="post" action="{% url 'projetos:aprovar_projeto' projeto.pk %}">
                    {% csrf_token %}
                    <button type="submit" class="btn btn-primary btn-sm">Aprovar</button>
                  </form>
                </div>
```

por:

```html
              {% elif projeto.status == "APROVADO_COM_RESSALVAS" and projeto.etapa == "TCC_II" %}
                <div class="mt-2">
                  <a href="{% url 'bancas:correcoes' projeto.pk %}" class="btn btn-outline btn-sm">
                    Gerenciar correções
                  </a>
                </div>
              {% elif projeto.status == "APROVADO_COM_RESSALVAS" %}
                <div class="mt-2">
                  <form method="post" action="{% url 'projetos:aprovar_projeto' projeto.pk %}">
                    {% csrf_token %}
                    <button type="submit" class="btn btn-primary btn-sm">Aprovar</button>
                  </form>
                </div>
```

(Dois `elif` separados, não um só com `or`: o TCC_II mostra um LINK
diferente, não o mesmo botão — a ordem entre os dois novos ramos não
importa entre si, mas os dois precisam vir antes do `elif
projeto.banca_ativa` que trata REPROVADO, exatamente como os ramos do Bloco
E já fazem.)

- [ ] **Passo 5: Rodar e confirmar que os testes passam**

Run: `docker compose exec web pytest apps/projetos/tests/test_orientacoes_tcc_ii.py -v`
Expected: todos `PASSED`.

- [ ] **Passo 6: Rodar a suíte de `apps/projetos` inteira**

Run: `docker compose exec web pytest apps/projetos/ -v`
Expected: todos `PASSED`.

- [ ] **Passo 7: `ruff` e `black`, depois commit**

```bash
docker compose exec web ruff check .
docker compose exec web black .
git add templates/projetos/orientacoes.html apps/projetos/tests/test_orientacoes_tcc_ii.py
git commit -m "Tarefa 8: /orientacoes/ mostra coorientador e correcoes do TCC_II"
```

---

## Tarefa 9: Rotas transversais e suíte de acessibilidade completa

**Arquivos:**
- Modificar: `conftest.py`.

**Interfaces:**
- Consome: `/temas/tcc-ii/criar/` (Tarefa 3), `/bancas/<id>/correcoes/`
  (Tarefa 4).

- [ ] **Passo 1: Ler o final de `conftest.py`**

Confira o formato de `Rota`/o mecanismo de `caminho` callable via atributo
solto no `Usuario` (`usuario.tema_id_para_rota`, T6;
`usuario.projeto_id_para_rota`/`banca_id_para_rota`, Bloco D) — mesmo
mecanismo desta tarefa.

- [ ] **Passo 2: Acrescentar as duas fábricas**

Perto de `cria_sugrad_com_ata_pendente_para_rotas` (Bloco E):

```python
def cria_professor_para_criar_tcc_ii_para_rotas():
    """Fábrica de `/temas/tcc-ii/criar/` (Bloco F): só precisa de um
    professor com `PerfilProfessor` — a tela não depende de nenhum aluno
    pré-existente (o `<select>` lista todo `PerfilAluno`, e a suíte não
    precisa que a lista tenha itens pra medir a tela)."""
    from apps.contas.models import PerfilProfessor, Usuario

    usuario = Usuario.objects.create_user(
        email="professor-criar-tccii-das-rotas@ufsm.br",
        password="x",
        nome_completo="Professor Criar TCC II das Rotas",
        cpf=_gera_cpf_das_rotas(37),
    )
    PerfilProfessor.objects.create(usuario=usuario, siape="1000025")
    return usuario


def cria_professor_com_correcao_para_rotas():
    """Fábrica de `/bancas/<projeto_id>/correcoes/` (Bloco F): professor
    orientador com um `Projeto` TCC_II `Aprovado com Ressalvas` e um
    `ItemCorrecao` já criado, pra medir o ramo "concluído"/"não concluído"
    do template."""
    from apps.bancas.models import ItemCorrecao
    from apps.comum.semestre import semestre_vigente
    from apps.contas.models import PerfilAluno, PerfilProfessor, Usuario
    from apps.projetos.models import Projeto

    orientador = Usuario.objects.create_user(
        email="professor-correcao-das-rotas@ufsm.br",
        password="x",
        nome_completo="Professor Correção das Rotas",
        cpf=_gera_cpf_das_rotas(38),
    )
    PerfilProfessor.objects.create(usuario=orientador, siape="1000026")

    aluno = Usuario.objects.create_user(
        email="aluno-correcao-das-rotas@ufsm.br",
        password="x",
        nome_completo="Aluno Correção das Rotas",
        cpf=_gera_cpf_das_rotas(39),
        papel=Usuario.ALUNO,
    )
    PerfilAluno.objects.create(usuario=aluno, matricula="2026399922")

    ano, periodo = semestre_vigente()
    projeto = Projeto.objects.create(
        aluno=aluno,
        orientador=orientador,
        etapa=Projeto.TCC_II,
        status=Projeto.APROVADO_COM_RESSALVAS,
        ano=ano,
        periodo=periodo,
    )
    ItemCorrecao.objects.create(projeto=projeto, descricao="Item das rotas.")

    orientador.projeto_id_para_rota = projeto.pk
    return orientador
```

- [ ] **Passo 3: Acrescentar as duas `Rota`**

Ao final de `ROTAS`:

```python
    Rota(
        "/temas/tcc-ii/criar/",
        "form",
        fabrica_usuario=cria_professor_para_criar_tcc_ii_para_rotas,
        h1="Criar TCC II",
    ),
    Rota(
        lambda usuario: f"/bancas/{usuario.projeto_id_para_rota}/correcoes/",
        "form",
        fabrica_usuario=cria_professor_com_correcao_para_rotas,
        h1="Correções — Aluno Correção das Rotas",
    ),
```

(O `h1` de `correcoes.html` é `Correções — {{ projeto.aluno.nome_completo }}`
— confira que o texto bate exatamente com o nome usado na fábrica ao rodar
o Passo 4; ajuste a string aqui se divergir.)

- [ ] **Passo 4: Rodar as cinco suítes transversais completas**

`orientacoes.html`/`meu_tcc.html` também mudaram (Tarefas 5 e 8) — roda
tudo, não filtrado:

Run: `docker compose exec web pytest tests/test_acessibilidade.py tests/test_toque.py tests/test_responsivo.py tests/test_teclado.py tests/test_rotas.py -v`
Expected: todos `PASSED`. Se `color-contrast` ou alvo de toque reprovarem
em algum elemento novo, ajuste a classe DaisyUI do elemento (mesmo tipo de
correção do Bloco C, Tarefa 4) e rode de novo até passar.

- [ ] **Passo 5: Rodar a suíte inteira**

Run: `docker compose exec web pytest`
Expected: todos `PASSED`.

- [ ] **Passo 6: `ruff` e `black`**

Run: `docker compose exec web ruff check .`
Run: `docker compose exec web black --check .`

- [ ] **Passo 7: Commit**

```bash
git add conftest.py
git commit -m "Tarefa 9: rotas transversais das telas do Bloco F"
```

---

## Ao concluir

Depois da Tarefa 9, verifique os 11 critérios de aceitação do spec (§11) um
a um, com evidência real — mesmo formato dos Blocos C, D e E. Atualize
`CLAUDE.md`: marque o Bloco F como concluído (mesmo formato das entradas
anteriores), atualize a seção "Ciclo de Vida e Status do TCC" removendo a
frase que ainda descreve o TCC_II como não implementado, e a descrição de
`apps/projetos`/`apps/bancas` na "Estrutura de Apps" para mencionar
`TermoPublicacao`/`ItemCorrecao`/`criar_tcc_ii_*`. Depois, invoque
`superpowers:finishing-a-development-branch` para a branch deste bloco.

Com o Bloco F concluído, o ciclo de vida do curso inteiro (TCC I e TCC II,
`Em Andamento` a `Concluído`) está implementado. Restam os Blocos G
(catálogo e calendário públicos) e H (API DRF).
